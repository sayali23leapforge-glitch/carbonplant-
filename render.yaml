services:
  - type: web
    name: carbonplant-web
    env: node
    nodeVersion: 24.14.1
    packageManager: npm
    installCommand: npm ci --include=dev
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
